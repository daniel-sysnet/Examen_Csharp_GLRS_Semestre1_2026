@model IEnumerable<Inscription>

@{
    ViewData["Title"] = "Lister les Inscriptions";
}

<div class="container mt-5">
    <div class="row mb-4">
        <div class="col-md-8">
            <h2>@ViewData["Title"]</h2>
        </div>
        <div class="col-md-4 text-end">
            <a href="@Url.Action("Creer", "Inscription")" class="btn btn-primary">Nouvelle Inscription</a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="mb-4">
        <h5>Filtrer par Classe:</h5>
        <div class="btn-group" role="group">
            <a href="@Url.Action("Lister", "Inscription")" class="btn btn-outline-secondary">Toutes les Classes</a>
            @foreach (var classe in ViewBag.Classes)
            {
                <a href="@Url.Action("ListerParClasse", "Inscription", new { classeId = classe.Id })" 
                   class="btn btn-outline-secondary">
                    @classe.Code
                </a>
            }
        </div>
    </div>

    @if (Model?.Any() == true)
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Date d'Inscription</th>
                        <th>Étudiant</th>
                        <th>Classe</th>
                        <th>Année Scolaire</th>
                        <th>Montant</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var inscription in Model)
                    {
                        <tr>
                            <td>@inscription.Date.ToString("dd/MM/yyyy")</td>
                            <td>@inscription.Etudiant?.Nom @inscription.Etudiant?.Prenom</td>
                            <td>@inscription.Classe?.Code</td>
                            <td>@inscription.AnneeScolaire?.Code</td>
                            <td class="text-end">@inscription.Montant.ToString("C")</td>
                            <td>
                                <a href="@Url.Action("Supprimer", "Inscription", new { id = inscription.Id })" 
                                   class="btn btn-sm btn-danger">
                                    Supprimer
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Aucune inscription trouvée.
        </div>
    }
</div>
